# 单因子回测框架开发文档

## 目录

1. [项目概述](#1-项目概述)
2. [框架架构设计](#2-框架架构设计)
3. [模块说明](#3-模块说明)
4. [数据规范](#4-数据规范)
5. [因子定义与计算](#5-因子定义与计算)
6. [因子预处理流程](#6-因子预处理流程)
7. [分层回测方法](#7-分层回测方法)
8. [因子评价指标](#8-因子评价指标)
9. [代码实现示例](#9-代码实现示例)
10. [使用示例](#10-使用示例)
11. [结果输出规范](#11-结果输出规范)
12. [注意事项与风险提示](#12-注意事项与风险提示)

---

## 1. 项目概述

### 1.1 背景

单因子测试（Single Factor Test）是量化投资研究的基础环节，用于评估某一特定因子对股票截面收益的预测能力。本框架提供标准化的单因子回测流程，涵盖数据处理、因子计算、分层测试、IC分析及绩效归因等完整链路。

### 1.2 目标

- 提供可复用、可扩展的单因子测试基础设施
- 统一因子预处理标准（去极值、中性化、标准化）
- 支持分层回测、IC/IR 分析、多空组合构建
- 输出规范化的因子评价报告

### 1.3 技术栈

| 组件 | 版本要求 |
|------|----------|
| Python | >= 3.9 |
| pandas | >= 1.5 |
| numpy | >= 1.23 |
| scipy | >= 1.9 |
| statsmodels | >= 0.13 |
| matplotlib | >= 3.6 |
| seaborn | >= 0.12 |

---

## 2. 框架架构设计

```
single_factor_framework/
├── data/
│   ├── loader.py          # 数据加载模块
│   ├── cleaner.py         # 数据清洗模块
│   └── universe.py        # 股票池筛选模块
├── factor/
│   ├── base.py            # 因子基类
│   ├── preprocessor.py    # 因子预处理（去极值/中性化/标准化）
│   └── calculator.py      # 因子计算工具
├── backtest/
│   ├── layered.py         # 分层回测引擎
│   ├── long_short.py      # 多空组合回测
│   └── portfolio.py       # 组合绩效计算
├── evaluation/
│   ├── ic_analysis.py     # IC/IR 分析
│   ├── return_analysis.py # 收益分析
│   └── metrics.py        # 评价指标汇总
├── report/
│   ├── plotter.py         # 图表绘制
│   └── generator.py       # 报告生成
├── config.py              # 全局配置
└── main.py                # 主入口
```

### 2.1 核心数据流

```
原始数据
   │
   ▼
数据清洗 & 股票池过滤
   │
   ▼
因子值计算
   │
   ▼
因子预处理（去极值 → 行业市值中性化 → 标准化）
   │
   ├──────────────────────────────┐
   ▼                              ▼
IC/IR 分析                   分层回测
   │                              │
   ▼                              ▼
因子有效性评估              各层净值曲线 & 多空组合
   │                              │
   └──────────────┬───────────────┘
                  ▼
           综合评价报告
```

---

## 3. 模块说明

### 3.1 数据加载模块（data/loader.py）

负责从数据源（本地CSV、数据库、Wind/Tushare API）加载行情数据、财务数据、行业分类数据。

**核心接口：**

```python
class DataLoader:
    def load_price(self, start_date: str, end_date: str, universe: list[str]) -> pd.DataFrame
    def load_financial(self, fields: list[str], period: str) -> pd.DataFrame
    def load_industry(self, standard: str = "SW_L1") -> pd.DataFrame
    def load_market_cap(self, start_date: str, end_date: str) -> pd.DataFrame
```

**返回数据格式（宽表）：**

```
          000001.SZ  000002.SZ  ...  600519.SH
date
2023-01-03   12.50      18.30  ...    1750.00
2023-01-04   12.60      18.10  ...    1760.00
```

### 3.2 股票池筛选模块（data/universe.py）

**筛选规则（默认）：**

| 规则 | 说明 |
|------|------|
| 剔除 ST/\*ST | 排除风险警示股 |
| 剔除上市不足 N 日 | 默认 N=252，避免新股效应 |
| 剔除涨跌停 | 排除无法成交日 |
| 剔除流动性不足 | 过去20日日均成交额 < 阈值（默认500万） |
| 剔除财务数据缺失 | 当期因子值为空 |

### 3.3 因子基类（factor/base.py）

所有因子继承此基类，统一接口：

```python
class BaseFactor:
    name: str           # 因子名称
    frequency: str      # 调仓频率，如 "M"（月）、"W"（周）
    direction: int      # 因子方向：1=正向，-1=负向

    def compute(self, data: dict) -> pd.DataFrame:
        """返回 (date, stock_code) 双索引的因子值 DataFrame"""
        raise NotImplementedError
```

### 3.4 分层回测引擎（backtest/layered.py）

将全体股票按因子值等分为 N 层（默认5层），分别计算各层组合的净值及收益序列。

**核心参数：**

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| n_groups | int | 5 | 分层数量 |
| rebalance_freq | str | "M" | 调仓频率 |
| weighting | str | "equal" | 组合加权方式（equal/cap_weighted） |
| cost | float | 0.002 | 单边交易成本 |
| benchmark | str | "000300.SH" | 基准指数 |

---

## 4. 数据规范

### 4.1 价格数据规范

| 字段 | 类型 | 说明 |
|------|------|------|
| date | datetime | 交易日期 |
| stock_code | str | 股票代码（含市场后缀） |
| open | float | 开盘价（前复权） |
| high | float | 最高价（前复权） |
| low | float | 最低价（前复权） |
| close | float | 收盘价（前复权） |
| volume | float | 成交量（股） |
| amount | float | 成交额（元） |
| adj_factor | float | 复权因子 |

### 4.2 因子数据规范

因子数据以 `(date, stock_code)` 为双索引，`factor_value` 为值列：

```
                         factor_value
date       stock_code
2023-01-31 000001.SZ        0.85
           000002.SZ       -1.23
           ...
           600519.SH        2.10
```

### 4.3 日期对齐规则

- **因子计算期**：使用期末财务数据（如季报披露截止日后一个交易日生效）
- **收益计算期**：持有期为调仓日次日开盘至下一调仓日开盘（避免未来数据）
- **对齐方式**：因子值 T 期 → 持有 T+1 至 T+2 期间收益

```
T（调仓日）    T+1（持仓起始）    T+2（下一调仓日）
    │                │                 │
    ├── 计算因子 ──▶│── 持有收益 ────▶│
```

---

## 5. 因子定义与计算

### 5.1 因子分类

| 大类 | 子类 | 示例因子 |
|------|------|----------|
| 价值类 | 估值 | PE_TTM、PB、PS_TTM、EV/EBITDA |
| 成长类 | 盈利增速 | 净利润同比增速、营收同比增速 |
| 质量类 | 盈利质量 | ROE_TTM、ROIC、毛利率、净利率 |
| 动量类 | 价格动量 | 过去1/3/6/12月涨跌幅 |
| 波动类 | 风险 | 历史波动率、Beta、最大回撤 |
| 流动性 | 交易 | 换手率、Amihud非流动性指标 |
| 技术类 | 技术指标 | RSI、MACD、布林带位置 |

### 5.2 因子计算示例（ROE 因子）

```python
class ROEFactor(BaseFactor):
    name = "ROE_TTM"
    frequency = "M"
    direction = 1  # ROE越高越好

    def compute(self, data: dict) -> pd.DataFrame:
        net_profit = data["net_profit_ttm"]   # TTM净利润
        equity = data["total_equity"]          # 归母净资产
        factor = net_profit / equity
        return factor.stack().rename("factor_value").to_frame()
```

---

## 6. 因子预处理流程

预处理严格按以下顺序执行：

```
原始因子值
    │
    ▼  Step 1
去极值（Winsorize）
    │
    ▼  Step 2
行业 & 市值中性化（OLS 回归残差）
    │
    ▼  Step 3
截面标准化（Z-Score）
    │
    ▼
预处理后因子值
```

### 6.1 Step 1：去极值

**方法 A — MAD法（默认）：**

$$\text{median\_abs\_dev} = \text{median}(|X_i - \text{median}(X)|)$$

将超出 $[\text{median} - n \times MAD,\ \text{median} + n \times MAD]$ 的值截断至边界，默认 $n=5$。

**方法 B — 百分位法：**

截断至 $[p_{\text{low}}, p_{\text{high}}]$ 百分位，默认 $[1\%, 99\%]$。

```python
def winsorize_mad(factor: pd.Series, n: float = 5.0) -> pd.Series:
    median = factor.median()
    mad = (factor - median).abs().median()
    upper = median + n * mad
    lower = median - n * mad
    return factor.clip(lower=lower, upper=upper)
```

### 6.2 Step 2：行业与市值中性化

对因子值做截面 OLS 回归，取残差作为中性化后的因子：

$$f_i = \alpha + \sum_{k=1}^{K} \beta_k \cdot \text{IndDummy}_{i,k} + \gamma \cdot \ln(\text{MktCap}_i) + \varepsilon_i$$

残差 $\varepsilon_i$ 即为中性化后的因子值。

```python
def neutralize(factor: pd.Series, industry_dummies: pd.DataFrame,
               log_mkt_cap: pd.Series) -> pd.Series:
    X = pd.concat([industry_dummies, log_mkt_cap.rename("ln_cap")], axis=1)
    X = sm.add_constant(X)
    model = sm.OLS(factor, X, missing="drop").fit()
    return model.resid
```

### 6.3 Step 3：截面标准化

$$z_i = \frac{f_i - \mu_f}{\sigma_f}$$

```python
def standardize(factor: pd.Series) -> pd.Series:
    return (factor - factor.mean()) / factor.std()
```

---

## 7. 分层回测方法

### 7.1 分层逻辑

每个调仓日，将当期有效股票按预处理后因子值从小到大排序，等分为 N 组（默认 N=5，Q1 因子值最小，Q5 最大）。

```
因子值 ──排序──▶ [Q1 | Q2 | Q3 | Q4 | Q5]
                  ↑最小值             ↑最大值
```

### 7.2 收益计算

对于第 $g$ 组，第 $t$ 期的等权组合收益为：

$$R_{g,t} = \frac{1}{N_g} \sum_{i \in g} r_{i,t}$$

其中 $r_{i,t}$ 为股票 $i$ 在持有期的对数收益率，$N_g$ 为第 $g$ 组股票数量。

### 7.3 多空组合

$$R_{\text{LS},t} = R_{Q5,t} - R_{Q1,t}$$

对于负向因子（`direction=-1`），多空组合反转：$R_{\text{LS},t} = R_{Q1,t} - R_{Q5,t}$

### 7.4 超额收益

$$\alpha_{g,t} = R_{g,t} - R_{\text{bench},t}$$

---

## 8. 因子评价指标

### 8.1 IC 分析（信息系数）

**Rank IC（Spearman相关系数）：**

$$\text{RankIC}_t = \text{Spearman}(f_{t-1}, r_t)$$

| 指标 | 计算公式 | 含义 | 参考标准 |
|------|----------|------|----------|
| IC Mean | $\bar{\text{IC}}$ | 因子平均预测能力 | \|均值\| > 0.03 为有效 |
| ICIR | $\bar{\text{IC}} / \sigma_{\text{IC}}$ | 因子稳定性 | \|ICIR\| > 0.5 较优 |
| IC > 0 比例 | $P(\text{IC}_t > 0)$ | 因子方向一致性 | > 55% 为优 |
| IC 衰减 | 滞后 1\~12 期 IC | 因子持续性 | 衰减越慢越好 |

### 8.2 分层收益分析

| 指标 | 计算公式 | 说明 |
|------|----------|------|
| 年化收益 | $(1+\bar{R})^{12/\text{freq}}-1$ | 月频调仓时 freq=1 |
| 年化波动 | $\sigma_R \times \sqrt{12}$ | 月频时乘以 $\sqrt{12}$ |
| 夏普比率 | $(\bar{R}-r_f)/\sigma_R \times \sqrt{12}$ | 年化夏普 |
| 最大回撤 | $\max_{\tau \leq t}(V_\tau - V_t)/V_\tau$ | 历史最大回撤 |
| 卡玛比率 | 年化收益 / 最大回撤 | 收益回撤比 |
| 胜率 | 超额收益 > 0 期数比例 | 跑赢基准概率 |

### 8.3 单调性检验

好的因子应体现各层收益单调递增（正向因子）或递减（负向因子），使用 **Spearman 秩相关** 检验各层平均超额收益与分组序号的相关性：

$$\rho = \text{Spearman}(\{1,2,...,N\},\ \{\bar{\alpha}_1,...,\bar{\alpha}_N\})$$

$\rho$ 接近 1（正向因子）或 -1（负向因子）表示单调性强。

### 8.4 多空组合绩效

| 指标 | 含义 |
|------|------|
| 年化多空收益 | 多空组合年化绝对收益 |
| 多空夏普 | 多空组合夏普比率 |
| 多空最大回撤 | 多空组合最大回撤 |
| 月度胜率 | 多空月度收益 > 0 的比例 |

---

## 9. 代码实现示例

### 9.1 完整框架核心代码

```python
# config.py
from dataclasses import dataclass, field

@dataclass
class FactorTestConfig:
    start_date: str = "2018-01-01"
    end_date: str = "2023-12-31"
    universe: str = "CSI800"           # 股票池
    rebalance_freq: str = "M"          # 调仓频率
    n_groups: int = 5                  # 分层数
    weighting: str = "equal"           # 权重方式
    transaction_cost: float = 0.002    # 单边交易成本
    benchmark: str = "000300.SH"       # 基准
    neutralize_industry: bool = True   # 是否行业中性
    neutralize_mktcap: bool = True     # 是否市值中性
    winsorize_method: str = "mad"      # 去极值方法
    winsorize_n: float = 5.0           # MAD倍数
    risk_free_rate: float = 0.025      # 年化无风险利率
```

```python
# factor/preprocessor.py
import pandas as pd
import numpy as np
import statsmodels.api as sm

class FactorPreprocessor:

    def __init__(self, config):
        self.config = config

    def process(self, factor: pd.DataFrame, industry: pd.DataFrame,
                mkt_cap: pd.DataFrame) -> pd.DataFrame:
        result = {}
        for date, group in factor.groupby(level="date"):
            f = group.droplevel("date").squeeze()
            f = self._winsorize(f)
            if self.config.neutralize_industry or self.config.neutralize_mktcap:
                ind = industry.loc[date] if date in industry.index else None
                cap = mkt_cap.loc[date] if date in mkt_cap.index else None
                f = self._neutralize(f, ind, cap)
            f = (f - f.mean()) / f.std()
            result[date] = f
        return pd.concat(result).rename_axis(["date", "stock_code"])

    def _winsorize(self, f: pd.Series) -> pd.Series:
        if self.config.winsorize_method == "mad":
            median = f.median()
            mad = (f - median).abs().median()
            n = self.config.winsorize_n
            return f.clip(median - n * mad, median + n * mad)
        else:
            return f.clip(f.quantile(0.01), f.quantile(0.99))

    def _neutralize(self, f: pd.Series, industry: pd.Series,
                    log_cap: pd.Series) -> pd.Series:
        controls = []
        if self.config.neutralize_industry and industry is not None:
            dummies = pd.get_dummies(industry, drop_first=True)
            controls.append(dummies)
        if self.config.neutralize_mktcap and log_cap is not None:
            controls.append(np.log(log_cap).rename("ln_cap"))
        if not controls:
            return f
        X = pd.concat(controls, axis=1).reindex(f.index).dropna()
        common = f.index.intersection(X.index)
        X_c, f_c = X.loc[common], f.loc[common]
        X_c = sm.add_constant(X_c)
        resid = sm.OLS(f_c, X_c).fit().resid
        return resid.reindex(f.index)
```

```python
# backtest/layered.py
import pandas as pd
import numpy as np

class LayeredBacktest:

    def __init__(self, config):
        self.config = config

    def run(self, factor: pd.DataFrame, returns: pd.DataFrame) -> dict:
        dates = factor.index.get_level_values("date").unique().sort_values()
        group_returns = {f"Q{i+1}": [] for i in range(self.config.n_groups)}
        group_returns["LS"] = []

        for i, t in enumerate(dates[:-1]):
            t_next = dates[i + 1]
            f_t = factor.xs(t, level="date").squeeze().dropna()

            # 分层
            labels = pd.qcut(f_t.rank(method="first"),
                              q=self.config.n_groups,
                              labels=[f"Q{j+1}" for j in range(self.config.n_groups)])

            # 计算持有期收益（t到t_next）
            ret = returns.loc[t:t_next].iloc[1:].mean()  # 简化示例

            for g in [f"Q{j+1}" for j in range(self.config.n_groups)]:
                stocks = labels[labels == g].index
                if len(stocks) == 0:
                    group_returns[g].append(np.nan)
                    continue
                r = ret.reindex(stocks).mean()
                # 扣除交易成本（换仓率 * 单边成本 * 2）
                r -= self.config.transaction_cost * 2 * 0.5  # 估算50%换仓率
                group_returns[g].append(r)

            # 多空组合
            ls = group_returns[f"Q{self.config.n_groups}"][-1] - group_returns["Q1"][-1]
            if hasattr(self.config, 'direction') and self.config.direction == -1:
                ls = -ls
            group_returns["LS"].append(ls)

        return {k: pd.Series(v, index=dates[:-1]) for k, v in group_returns.items()}
```

```python
# evaluation/ic_analysis.py
import pandas as pd
import numpy as np
from scipy import stats

class ICAnalysis:

    def compute_ic(self, factor: pd.DataFrame, forward_returns: pd.DataFrame,
                   method: str = "rank") -> pd.Series:
        ic_series = {}
        for date in factor.index.get_level_values("date").unique():
            try:
                f = factor.xs(date, level="date").squeeze()
                r = forward_returns.loc[date]
                common = f.index.intersection(r.index)
                if len(common) < 30:
                    continue
                f_c, r_c = f[common], r[common]
                if method == "rank":
                    ic, _ = stats.spearmanr(f_c, r_c)
                else:
                    ic, _ = stats.pearsonr(f_c, r_c)
                ic_series[date] = ic
            except Exception:
                continue
        return pd.Series(ic_series).sort_index()

    def summary(self, ic: pd.Series) -> dict:
        return {
            "IC_mean":        round(ic.mean(), 4),
            "IC_std":         round(ic.std(), 4),
            "ICIR":           round(ic.mean() / ic.std(), 4) if ic.std() != 0 else np.nan,
            "IC_positive_pct": round((ic > 0).mean(), 4),
            "IC_gt_002_pct":  round((ic.abs() > 0.02).mean(), 4),
            "t_stat":         round(stats.ttest_1samp(ic.dropna(), 0).statistic, 4),
            "p_value":        round(stats.ttest_1samp(ic.dropna(), 0).pvalue, 4),
        }
```

```python
# evaluation/metrics.py
import pandas as pd
import numpy as np

def calc_nav(returns: pd.Series) -> pd.Series:
    return (1 + returns).cumprod()

def annual_return(returns: pd.Series, freq: int = 12) -> float:
    total = (1 + returns).prod()
    n = len(returns)
    return total ** (freq / n) - 1

def annual_volatility(returns: pd.Series, freq: int = 12) -> float:
    return returns.std() * np.sqrt(freq)

def sharpe_ratio(returns: pd.Series, rf: float = 0.025, freq: int = 12) -> float:
    excess = returns - rf / freq
    if excess.std() == 0:
        return np.nan
    return excess.mean() / excess.std() * np.sqrt(freq)

def max_drawdown(returns: pd.Series) -> float:
    nav = calc_nav(returns)
    roll_max = nav.cummax()
    drawdown = (nav - roll_max) / roll_max
    return drawdown.min()

def calmar_ratio(returns: pd.Series, freq: int = 12) -> float:
    mdd = abs(max_drawdown(returns))
    if mdd == 0:
        return np.nan
    return annual_return(returns, freq) / mdd

def win_rate(returns: pd.Series) -> float:
    return (returns > 0).mean()

def performance_summary(returns: pd.Series, rf: float = 0.025,
                         freq: int = 12) -> dict:
    return {
        "年化收益率":   f"{annual_return(returns, freq):.2%}",
        "年化波动率":   f"{annual_volatility(returns, freq):.2%}",
        "夏普比率":     f"{sharpe_ratio(returns, rf, freq):.3f}",
        "最大回撤":     f"{max_drawdown(returns):.2%}",
        "卡玛比率":     f"{calmar_ratio(returns, freq):.3f}",
        "月度胜率":     f"{win_rate(returns):.2%}",
    }
```

### 9.2 主流程入口

```python
# main.py
from config import FactorTestConfig
from data.loader import DataLoader
from data.universe import UniverseFilter
from factor.preprocessor import FactorPreprocessor
from backtest.layered import LayeredBacktest
from evaluation.ic_analysis import ICAnalysis
from evaluation.metrics import performance_summary
from report.plotter import plot_layered_nav, plot_ic_series

def run_factor_test(factor_class, config: FactorTestConfig):
    # 1. 加载数据
    loader = DataLoader(config)
    price = loader.load_price()
    financial = loader.load_financial()
    industry = loader.load_industry()
    mkt_cap = loader.load_market_cap()

    # 2. 过滤股票池
    universe_filter = UniverseFilter(config)
    valid_universe = universe_filter.filter(price)

    # 3. 计算因子
    factor_obj = factor_class(config)
    raw_factor = factor_obj.compute({"price": price, "financial": financial})
    raw_factor = raw_factor[raw_factor.index.get_level_values("stock_code").isin(
        valid_universe.stack()[valid_universe.stack()].index.get_level_values(1)
    )]

    # 4. 因子预处理
    preprocessor = FactorPreprocessor(config)
    factor = preprocessor.process(raw_factor, industry, mkt_cap)

    # 5. 计算前向收益
    forward_returns = price.pct_change().shift(-1)

    # 6. IC 分析
    ic_analyzer = ICAnalysis()
    ic_series = ic_analyzer.compute_ic(factor, forward_returns)
    ic_summary = ic_analyzer.summary(ic_series)

    # 7. 分层回测
    backtester = LayeredBacktest(config)
    group_returns = backtester.run(factor, forward_returns)

    # 8. 绩效统计
    perf = {g: performance_summary(r) for g, r in group_returns.items()}

    # 9. 输出报告
    plot_layered_nav(group_returns, config)
    plot_ic_series(ic_series, config)

    return {
        "ic_summary": ic_summary,
        "performance": perf,
        "ic_series": ic_series,
        "group_returns": group_returns,
    }
```

---

## 10. 使用示例

### 10.1 测试 ROE 因子

```python
from main import run_factor_test
from factor.calculator import ROEFactor
from config import FactorTestConfig

config = FactorTestConfig(
    start_date="2018-01-01",
    end_date="2023-12-31",
    universe="CSI800",
    rebalance_freq="M",
    n_groups=5,
    transaction_cost=0.002,
    benchmark="000300.SH",
    neutralize_industry=True,
    neutralize_mktcap=True,
)

results = run_factor_test(ROEFactor, config)

# 打印 IC 汇总
print("=== IC 分析 ===")
for k, v in results["ic_summary"].items():
    print(f"  {k}: {v}")

# 打印各层绩效
print("\n=== 分层绩效 ===")
for group, metrics in results["performance"].items():
    print(f"\n[{group}]")
    for k, v in metrics.items():
        print(f"  {k}: {v}")
```

### 10.2 自定义因子

```python
from factor.base import BaseFactor
import pandas as pd

class MomentumFactor(BaseFactor):
    """过去 6 个月动量因子（排除最近 1 个月）"""
    name = "MOM_6M_1M"
    frequency = "M"
    direction = 1

    def compute(self, data: dict) -> pd.DataFrame:
        price = data["price"]
        ret_6m = price.pct_change(6)
        ret_1m = price.pct_change(1)
        factor = ret_6m - ret_1m   # 6个月收益减去最近1个月
        return factor.stack().rename("factor_value").to_frame()
```

---

## 11. 结果输出规范

### 11.1 标准输出图表

| 图表 | 说明 |
|------|------|
| 分层净值曲线 | 各分组及多空组合净值对比，附基准线 |
| IC 时序图 | 逐期 IC 值，附滚动均值（如12期移动平均） |
| IC 分布直方图 | IC 正态性检验，附均值/标准差注释 |
| 分层年化收益柱状图 | 各层平均年化超额收益，直观展示单调性 |
| IC 衰减曲线 | 滞后 1\~12 期 IC 均值，反映因子持续性 |
| 因子收益热力图 | 按年/月统计分层超额收益 |

### 11.2 评价报告结构

```
因子名称：ROE_TTM
测试区间：2018-01-01 ~ 2023-12-31
股票池：CSI800（沪深800）
调仓频率：月频
中性化：行业 + 市值

一、因子有效性（IC 分析）
┌──────────────────────────────────────────┐
│ IC 均值         │  0.0412                │
│ IC 标准差       │  0.0831                │
│ ICIR            │  0.4958                │
│ IC > 0 比例     │  67.86%                │
│ t 统计量        │  3.21（p=0.0019）      │
└──────────────────────────────────────────┘

二、分层回测绩效
         年化收益  年化波动  夏普  最大回撤  胜率
Q1       -3.21%   18.5%   -0.30  -42.1%   41%
Q2        1.18%   17.2%    0.01  -36.8%   48%
Q3        3.56%   16.9%    0.17  -33.2%   52%
Q4        5.89%   17.1%    0.29  -31.5%   55%
Q5        8.24%   17.8%    0.40  -28.9%   59%
多空(LS) 11.45%   12.1%    0.86  -18.3%   68%

三、单调性检验
Spearman ρ = 0.90（p=0.037），各层收益单调递增。

四、结论
ROE_TTM 因子在测试区间内具有显著预测能力（ICIR=0.50，t=3.21），
各层收益单调性良好，多空夏普达 0.86，为有效因子。
```

---

## 12. 注意事项与风险提示

### 12.1 数据处理注意事项

| 风险点 | 说明 | 应对措施 |
|--------|------|----------|
| 前视偏差（Look-ahead Bias） | 使用了未来数据 | 严格按财报披露日对齐；季报用实际披露日而非报告期 |
| 幸存者偏差（Survivorship Bias） | 仅使用当前存活股票 | 使用含退市股完整历史数据 |
| 极端值污染 | 异常值扭曲分布 | 每期截面去极值 |
| 行业集中 | 因子隐含行业暴露 | 行业中性化处理 |
| 市值效应混入 | 因子与市值高度相关 | 市值中性化处理 |

### 12.2 回测局限性

- **容量限制**：回测不考虑冲击成本，实盘中大规模资金的冲击成本可能显著侵蚀收益
- **交易成本估算**：固定费率假设；实际应区分买卖价差、冲击成本、印花税
- **仓位假设**：等权持仓与实际加权持仓存在差异
- **高频换仓**：月频以上换仓的交易成本需重新评估

### 12.3 因子失效风险

- 因子历史有效≠未来有效，需持续监控 IC 是否衰减
- 建议设置因子有效性滚动监控：每月计算最新 12 期滚动 ICIR，若降至 0.3 以下触发预警
- 考虑因子间的相关性，避免组合中使用高度相关的多个因子

### 12.4 建议检查清单

- [ ] 确认数据无前视偏差（因子生效日 vs. 财报披露日）
- [ ] 确认股票池包含历史退市股（无幸存者偏差）
- [ ] 每期截面有效样本数 >= 50
- [ ] 因子分布可视化检查（是否存在异常值集中）
- [ ] 行业分布均匀性检查（各行业覆盖是否充分）
- [ ] 子区间稳定性检验（将回测期分为前后两半分别检验）
- [ ] 不同市场环境（牛市/熊市/震荡市）下的因子表现分析

---

*文档版本：v1.0 | 日期：2026-05-31 | 作者：量化研究团队*
